# wrangler 로컬 캐시
.wrangler/

# 악보 이미지는 저작권 때문에 저장소에 넣지 않습니다 (로컬에만 보관)
raw/
data/

# 빌드 산출물 — data/ 에서 언제든 다시 만들 수 있습니다.
# 이미지를 깃 히스토리에 넣고 싶지 않다면 아래 두 줄을 켜두세요.
site/scores/
site/library.json
site/version.json

.DS_Store
node_modules/
